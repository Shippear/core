dev {

  email-notification {
    activated = false
  }

  cache {
    initial-delay: 10 seconds
    update-interval: 40 seconds
    activated: false
  }

  await-to{
    initial-delay: 10 seconds
    update-interval: 40 seconds
    activated = false
  }

  commission: 90

  mongodb {

    username = "shippear"
    password = "shippear"
    host = "localhost"
    port = 27017
    database = "shippear"

    uri = "mongodb://"${dev.mongodb.username}":"${dev.mongodb.password}"@"${dev.mongodb.host}":"${dev.mongodb.port}"/"${dev.mongodb.database}

  }
}



